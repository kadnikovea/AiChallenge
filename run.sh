#!/bin/bash
# Запуск основной программы Gradle без прогресс-бара (--console=plain)
# и с минимальным логированием (--quiet), чтобы в терминале было видно
# только ваш диалог "Вы>" / "Модель>".

exec ./gradlew run --console=plain --quiet "$@"
